# Replit Prompt — Roll out new petitmokus logo

Copy everything below into a new Replit AI chat (or paste into the Agent task box).
Attach the `brand/` folder from this asset pack alongside the prompt.

---

## TASK

Roll out the new petitmokus squirrel logo across the entire codebase — website, web app, emails, favicons, and any social/marketing surface — and remove every reference to the old logo. Use the assets in the attached `brand/` folder. Do not redraw the logo, edit the SVG, or alter the palette.

## CONTEXT

- The new logo is a warm-brown squirrel holding an acorn, in the petitmokus pastel palette.
- The current site already uses the same brand palette tokens (Sandy `#F9F6F1`, Primary `#D897A8`, Text `#5C4A3D`, etc.) — do not change those tokens.
- Logo-specific tones (fur `#B89478`, fur shadow `#8C6F58`, acorn `#E8C9A8`) live inside the SVG and don't need to be added as design tokens unless we use them elsewhere.

## FILES TO ADD

Copy the entire `brand/` folder into the project's static assets directory. Pick the correct destination based on the framework:

- Vite / React static: `public/brand/`
- Next.js: `public/brand/`
- Plain HTML site: `/brand/` at the repo root
- Express / static server: `static/brand/` or `public/brand/`

Also copy these favicon files **to the public root** (next to `index.html`) so they resolve at well-known paths:

- `brand/favicon/favicon.ico`        → `/favicon.ico`
- `brand/favicon/favicon.svg`        → `/favicon.svg`
- `brand/favicon/favicon-16x16.png`  → `/favicon-16x16.png`
- `brand/favicon/favicon-32x32.png`  → `/favicon-32x32.png`
- `brand/favicon/apple-touch-icon.png` → `/apple-touch-icon.png`

## CHANGES TO MAKE

### 1. HTML `<head>` — every page / template

Replace any existing `<link rel="icon" ...>`, `<link rel="apple-touch-icon" ...>`, `<meta name="theme-color" ...>` and similar lines with this exact block:

```html
<link rel="icon" href="/favicon.ico" sizes="any">
<link rel="icon" type="image/svg+xml" href="/favicon.svg">
<link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
<link rel="apple-touch-icon" href="/apple-touch-icon.png">
<meta name="theme-color" content="#F9F6F1">
```

If using Next.js App Router, place these in `app/layout.tsx` `metadata` / `<head>` instead. If using `react-helmet` or similar, update there.

### 2. Site header / nav — replace the old mark

Find every component that renders the existing logo (likely named `Logo`, `Brand`, `SiteHeader`, `Navbar`, etc.). Replace the image source with:

```html
<img src="/brand/svg/petitmokus-logo-color.svg" alt="petitmokus" width="40" height="40">
```

If the existing component is also rendering wordmark text next to the mark, leave the wordmark text alone — only the icon source changes. If the existing layout uses a single combined image, swap to `/brand/svg/petitmokus-lockup-horizontal.svg` instead.

### 3. Loading states / splash screens / 404 pages

Search for any other usage of the old logo image and replace with `/brand/svg/petitmokus-logo-color.svg`. Common spots: loading spinners, empty states, error pages, login screens, PDF receipts.

### 4. Email templates

For every transactional and marketing email template (look in `emails/`, `templates/`, `mail/`, or wherever Postmark/Sendgrid/Resend templates live), replace the logo `<img>` with the email-safe PNG hosted on the public site (transparent SVGs and PNGs render unreliably in Outlook + Gmail dark mode). Use:

```html
<img src="https://petitmokus.com/brand/png/email/petitmokus-logo-email-400.png"
     alt="petitmokus"
     width="120" height="120"
     style="display:block;border:0;outline:none;text-decoration:none;">
```

Sizes available: `200`, `400`, `600`. Pick `400` for headers, `600` for hero placement.

### 5. Open Graph / social previews

Update `og:image` and `twitter:image` references if they pointed at the old logo. The 1024px PNG works as a fallback social tile:

```html
<meta property="og:image" content="https://petitmokus.com/brand/png/petitmokus-logo-1024.png">
<meta name="twitter:image" content="https://petitmokus.com/brand/png/petitmokus-logo-1024.png">
```

(If the site already has dedicated OG art, leave it alone — only update if it was using the logo as the OG image.)

### 6. PWA manifest (if present)

If `manifest.json` / `manifest.webmanifest` references logo PNGs, update to:

```json
{
  "icons": [
    { "src": "/brand/png/petitmokus-logo-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/brand/png/petitmokus-logo-512.png", "sizes": "512x512", "type": "image/png" }
  ],
  "theme_color": "#F9F6F1",
  "background_color": "#F9F6F1"
}
```

### 7. README / docs

If the project README has a logo at the top, swap the image src to `brand/svg/petitmokus-logo-color.svg` (or a GitHub-raw URL once the brand folder is committed).

## CLEANUP

- Delete the old logo files from the repo (search for `logo.png`, `logo.svg`, `petitmokus-old*`, anything in `assets/logo/` that isn't from the new `brand/` folder).
- Run a project-wide search for any remaining hardcoded paths to old logo files and update them.
- Check `next/image` or similar config for whitelisted logo paths and update if needed.

## VERIFY

Before finishing, confirm:

1. `curl -I https://<dev-url>/favicon.ico` returns 200
2. `curl -I https://<dev-url>/brand/svg/petitmokus-logo-color.svg` returns 200
3. The browser tab shows the new squirrel favicon (hard refresh required)
4. The site header renders the new mark
5. A test email (use the password-reset / welcome flow) renders the new logo against beige
6. There are no remaining references to the old logo anywhere in the codebase (`grep -r` for old filenames)

## DO NOT

- Do not modify any SVG inside `brand/svg/` — they are final.
- Do not change the existing palette tokens or component styling.
- Do not rasterize the SVGs further or re-export PNGs — use the provided sizes.
- Do not introduce new CSS for the logo beyond `width` / `height` / `display` properties.

When done, summarize: list the files added, the components/templates updated, and the old logo files deleted.
